.drag-ghost {
    position: absolute;
    width: 56px;
    height: 56px;
    display: none;
    overflow: hidden;
}

.drag-ghost-icon {
    width: 56px;
    height: 56px;
}

.drag-ghost-rarity {
    position: absolute;
    width: 100%;
    height: 100%;
}

.drag-active {
    opacity: 0.4;
}
